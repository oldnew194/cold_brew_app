<h1>Features#index</h1>
<p>Find me in app/views/features/index.html.erb</p>

<% content_for(:title, t('.title')) %>
<div class="container pt-3">
  <!-- 掲示板一覧 -->
  <div class="row">
    <div class="col-12">
      <div class="row">

        <% if @store_features.present? %>
        <%= render @features %> 
        <% else %>
        <p><%= t('.no_result') %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>