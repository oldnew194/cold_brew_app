<% breadcrumb :store_index %>
<% breadcrumb :store_index, @q %>
<% content_for(:title, t('.title')) %>
<div class="container pt-3">
  <!-- 掲示板一覧 -->
  すべて    (<%= @stores_count %>件)
  <div class="row">
    <div class="col-12">
      <div class="row">
        <%= render @stores %> 
      </div>
      <!-- ページネーション -->
      <%= paginate @stores %>
    </div>
    
  </div>
</div>