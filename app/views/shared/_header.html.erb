<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark >
    <div class="navbar-brand mb-0 h1">
      <%= link_to root_path, class: 'navbar-brand' do %>  
        <span>Cold B/Crew</span>
      <% end %>
    </div>
    
    <button class="navbar-toggler" data-toggle="collapse" data-target='#navbarSupportedContent' aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse justify-content-center">
      <% unless request.path == '/' %>
        <font color="white">店名から探す</font>
        <%= search_form_for @search_store, url: stores_path do |f| %>
          <%= f.text_field :name_cont ,placeholder: '店名' %>
          <%= f.submit class: 'btn btn-light btn-sm'%>
        <% end %>

        <font color="white">味の評価から探す</font>
        <table>
          ☕️
          <%= search_form_for @search_article, url: search_store_articles_path(store_id: @search_articles) do |f| %>
            <div class="rate_eq_h">
              <%= f.radio_button(:rate_eq, '1') %>
              <%= f.label(:rate_eq, '1', value: 1) %>
              <%= f.radio_button(:rate_eq, '2') %>
              <%= f.label(:rate_eq, '2', value: 2) %>
              <%= f.radio_button(:rate_eq, '3') %>
              <%= f.label(:rate_eq, '3', value: 3) %>
              <%= f.radio_button(:rate_eq, '4') %>
              <%= f.label(:rate_eq, '4', value: 4) %>
              <%= f.radio_button(:rate_eq, '5') %>
              <%= f.label(:rate_eq, '5', value: 5) %>
            </div>
            🍒
            <%= f.submit class: 'btn btn-light btn-sm'%>    
          <% end %>  
        </table>
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto main-nav align-items-center">

        <li class="nav-item dropdown dropdown-slide">
          <a href="#" class="nav-link" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" id="header-profile">
            <%= image_tag current_user.avatar_url, size: '45x45', class: 'rounded mr15'%>
          </a>
          <div class="dropdown-menu dropdown-menu-right">
            <%= link_to current_user.name, user_path(current_user), class: 'dropdown-item' %>
            <%= link_to 'ブックマーク一覧', favorites_stores_path, class: 'dropdown-item' %>
            
            <%= link_to (t 'defaults.logout'), logout_path, class: 'dropdown-item', method: :delete %>
          </div>
        </li>
      </ul>
    </div>
  </nav>
</header>